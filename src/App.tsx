import React from "react";
import { BrowserRouter } from "react-router-dom";
import { Router } from "./ui/molecules";

export const App = () => (
  <BrowserRouter>
    <Router />
  </BrowserRouter>
);
